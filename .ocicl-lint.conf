# ocicl linter configuration for Happening

# Suppress false positive in macro templates
suppress-rules = malformed-let

# Suggest utilities from common libraries
suggest-libraries = alexandria, uiop
